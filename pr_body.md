## 📌 관련 이슈
- #이슈번호

## ✨ 변경 내용
- 프론트엔드(3팀)와의 E2E 연동 테스트를 위한 `socket-service` WebSocket(STOMP) Mock API 구현
- `WebSocketConfig.kt`: Spring STOMP 엔드포인트(`/ws/parkit`) 및 브로커(`/topic`, `/app`) 설정 활성화
- `MockCoachingController.kt`: `@Scheduled`를 활용하여 1초 간격으로 가짜 주차 코칭 데이터를 `/topic/coaching` 채널로 브로드캐스팅
- 기존의 불필요한 WebFlux Raw WebSocket 핸들러(`CoachingSocketHandler.kt`) 제거

## ✅ 체크리스트
- [x] 빌드 및 테스트를 통과했나요?
- [x] 불필요한 로그나 주석은 제거했나요?

## 📝 리뷰 포인트
- 3팀 프론트엔드 파트에서 `ws://localhost:8082/ws/parkit` 엔드포인트로 연결 가능한지 확인 부탁드립니다.
- 실제 데이터의 JSON 스키마(센서 ID, 거리, 경고 레벨, 메시지, 타임스탬프)에 대해 프론트엔드 팀에서 파싱 이슈 없는지 검증 필요합니다.
